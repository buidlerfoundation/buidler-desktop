const initialState = {};

const configReducers = (state = initialState, action) => {
  const { type } = action;
  switch (type) {
    default:
      return state;
  }
};

export default configReducers;
